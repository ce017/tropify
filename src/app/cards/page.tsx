import { Cards } from "@/components/Cards";

/** the pmndrs example on its own, with nothing else competing for WebGL */
export default function CardsPage() {
  return (
    <section className="cards-stage">
      <Cards />
    </section>
  );
}
